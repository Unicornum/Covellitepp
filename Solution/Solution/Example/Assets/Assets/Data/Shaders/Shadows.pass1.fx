
void psShadow(Pixel _Value)
{
  // void for write to depth buffer only.

  //float Z = _Value.WorldPos.z / _Value.WorldPos.w;
  //float Depth = LinearDepthFromNearToFar(Z, CameraData.Projection);
  //return CalculateColorFromDept(Depth, 7.0f, 15.0f);
}
